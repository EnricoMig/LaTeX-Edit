services:
  latexedit:
    build:
      context: .
      dockerfile: Dockerfile
    image: latexedit:local
    container_name: latexedit
    restart: unless-stopped
    ports:
      - "8095:8443"
      - "8096:8080"
    environment:
      LATEX_ENGINE: pdflatex
      LATEX_TIMEOUTSECONDS: "120"
      LATEX_PASSES: "2"
      LATEX_WORKSPACEROOT: /data/workspace
      TLS_SAN_EXTRA: "DNS:YOUR_NAS_HOST"
      TZ: America/Sao_Paulo
    volumes:
      # Biblioteca de projetos LaTeX no NAS
      - /DATA/Documents/Biblioteca:/data/workspace
    # Não use "cpus:" junto com deploy.resources (Compose/CasaOS conflita)
    mem_limit: 2g
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/api/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 90s
